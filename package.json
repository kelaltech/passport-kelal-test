{
  "name": "passport-kelal-test",
  "version": "0.0.0",
  "description":
    "Demo for Kelal Profile's OAuth 2.0 App Connection feature using 'passport-kelal'.",
  "main": "dist/main.js",
  "scripts": {
    "clean": "node_modules/.bin/del dist",
    "build": "yarn clean && yarn tsc && yarn lint",
    "watch": "yarn clean && yarn tsc -w",
    "develop": "nodemon",
    "lint": "prettier --write ./src/**/*.ts ./*.{json,md}",
    "start": "yarn build && node ."
  },
  "repository": "git@gitlab.com:kelal/dev/passport-kelal-test.git",
  "author": "Kaleab Melkie <kaleabmelkie@gmail.com>",
  "dependencies": {
    "koa-passport": "^4.0.1",
    "meseret": "^1.7.6",
    "passport-kelal": "^0.3.0"
  },
  "devDependencies": {
    "@types/koa-passport": "^4.0.1"
  }
}
